import PlaceholderPage from '../components/PlaceholderPage'

export default function NotFoundPage() {
  return (
    <PlaceholderPage
      title="Página no encontrada"
      emoji="404"
      description="La ruta que buscas no existe. Regresa al catálogo y sigue tu camino."
    />
  )
}
